[project]
name = "estimator-api"
version = "0.1.0"
description = "Property Value Estimator API (App 1 backend)"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py312"
